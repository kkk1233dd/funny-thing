# 学习笔记
## 节点一：vs code系列快速操作指令

### 跳转定义    

```
1.鼠标选中类--选中后右键
#跳转定义：跳转到类、属性等具体实现定义的位置
#跳转声明：一般是跳转到头文件中
#跳转类型定义：跳转到类型定义的位置，如结构体、枚举等，一般是自定义类型后，无法直接查看时需要使用
#转到引用：跳转到所有使用该属性的位置，就像是ctrl+f一样，会搜索所有使用该属性的位置

2.鼠标悬停--选中后点击，过一段时间
```








## 节点二：github配置操作
### 1.配置github账号（！XXX替换自己的ssh密钥，此处为示例）

这一步我直接通过c盘创建一个config文件，实现了全局连接，在vs code系列文件中，不需要重复配置

配置参考：

```
Host github.com
    HostName ssh.github.com
    Port 443
    User git
    IdentityFile ~/.ssh/id_edXXXXX  
    IdentitiesOnly yes
    StrictHostKeyChecking accept-new
```

### 2.配置本地git仓库

#### 1.创建”README.md“文件
    #该文件是项目描述文件，可以在文件中添加项目的描述、使用方法等，也包括项目的版本信息、作者信息等
#### 2.创建”gitignore“文件
##### 该文件是追踪/忽略设置文件，
    ```
        1.直接写/* 表示忽略目录下的所有文件
            如：build/* 表示生成输出的文件忽略追踪，可以保护git仓库不会非常臃肿
        2.写！+文件名 表示追踪该文件，也是最常见的操作，指定某个路径或文件处于追踪状态，后续可以修改这个文件
            如：!README.md 表示追踪README.md文件，可以保护git仓库不会丢失该文件
    ```
### 3.连接github仓库
#### 1.在github上创建一个仓库，如：xx-xxx
#### 2.创建一个新的分支
    我这里按照我的习惯，默认不用main分支
#### 3.先执行：git init指令
    执行结束后用ls -Force指令查看是否创建成功，git文件夹是否存在：该指令会自动创建一个隐藏文件夹”.git“，用force可以查看是否创建成功
#### 4.在本地git登记远程仓库信息：
    ```
    执行”git remote add origin git@github.com/用户名/仓库名.git“指令
        1.以本项目为例：git remote add origin git@github.com:kkk1233dd/funny-thing.git
        2.这个指令是修改本地git仓库记录的远程仓库文件地址，不会修改任何文件，甚至不会联网 
    ```
#### 5.下载远程仓库分支:git fetch origin
    ```
        1.git fetch origin 正式联网；从远程下载该分支的历史记录，并保存在git中——不会修改当前文件
        2.用git branch -r验证获取成功
    ```
#### 6.正式连接：git checkout -b Notes origin/Notes
    ```
        1.-b表示可以边连边创建新分支，在这里的作用，是在本地git上创建一个分支，基于origin/Notes分支
        2.git checkout 切换分支，直接切换到新的分支上去
        3.用git branch -VV验证连接成功 
        4.-VV表示显示更多内容，这里是显示”分支名 + 最新提交 + 跟踪关系 ： Notes e04cc2b [origin/Notes]“
     ```
#### 7.提交本地修改到远程仓库
    ```
        1.git add . 表示追踪所有文件
        2.git commit -m "提交信息" 表示提交修改，提交信息为”提交信息“
        3.git push origin Notes 表示将本地Notes分支提交到远程仓库Notes分支
    ```
### 4.其他操作
#### 1.生成私钥
```
ssh-keygen -t ed25519 -C "kkk1233dd@github" -f "$env:USERPROFILE\.ssh\id_ed25519_personal" -N ""

//-t表示加密计算方式
//-C表示注释，这里是邮箱
//-f表示文件名，这里是生成私钥存放位置
//-N表示密码，这里是空密码
```
输入后，会生成一个私钥文件

## 节点三：cmakelist.txt
### 1.找头文件
```
发现某个头文件缺失，报错标红
首先确定是哪里的引用
1.系统自带的文件
2.项目依赖文件
确定之后，
findstr /s /n /c:"#define _T(" C:\mingw64\x86_64-w64-mingw32\include\*.h
/s表示搜索子目录
/n表示显示行号
/c表示搜索内容（不写/c可能会导致正则表达式处理，导致搜索失败）
在某一个范围下，找指定的定义需要的头文件（比如这里是找_T定义的头文件）
```
## 节点四：c++学习
### 1.imgui学习
```
imgui是一套ui设计库，里面包括按钮文本框等控件，写起来和c#的winform类似
但是imgui不能直接使用，需要
1.用glfw管理窗口创建——winform可以直接创建、管理窗口，不存在中间商
2.需要或者说支持或者说，选择使用了opengl（也可以是dx11，vulkan等）来支持让gpu渲染窗口，增加渲染性能与速度
3.说是说三层架构，但是对于另外两个来说，支持imgui不是设计初衷，而是反过来，imgui设计本身就是依赖别的后端作为协助
```
### 2.glfw知识
glfw概念：
```
glfw是一个跨平台的窗口管理库，里面包括窗口创建、管理、事件处理等功能
是一个非常完善、应用广泛的库
``` 
glfw中的缓冲交换概念：
```
存在一个写入缓冲区（后缓冲区），和一个读取缓冲区（前缓冲区）
数据写入时先写入后缓冲，满足条件时，前后置换，将新画面显示出来，新数据再覆盖“后缓冲区”——设计初衷是为了防止画面割裂等不同步问题
交换意思是：交换指针，而不是交换数据，这样效率非常快，且数据可靠
```
## 节点五：vs code系列软件操作
```
选中多行，按下tab，可以整体右移
    选中多行，按下shift+tab，可以整体左移
选中多行，按下alt+上下，可以整体上下移动
选中多行，按下ctrl+k，再按下ctrl+f，可以格式化选中的代码
按下shift+alt+f，会格式化所有代码

```