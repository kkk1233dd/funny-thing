# 学习笔记
## 节点一：vs code系列快速操作指令

### 跳转定义    

```
1.鼠标选中类--选中后右键
#跳转定义：跳转到类、属性等具体实现定义的位置
#跳转声明：一般是跳转到头文件中
#跳转类型定义：跳转到类型定义的位置，如结构体、枚举等，一般是自定义类型后，无法直接查看时需要使用
#转到引用：跳转到所有使用该属性的位置，就像是ctrl+f一样，会搜索所有使用该属性的位置

2.鼠标悬停--选中后点击，过一段时间
```







## 节点二：cmakelist.txt

## 节点三：github配置操作
### 1.配置github账号（！XXX替换自己的ssh密钥，此处为示例）

这一步我直接通过c盘创建一个config文件，实现了全局连接，在vs code系列文件中，不需要重复配置

配置参考：

```
Host github.com
    HostName ssh.github.com
    Port 443
    User git
    IdentityFile ~/.ssh/id_edXXXXX  
    IdentitiesOnly yes
    StrictHostKeyChecking accept-new
```

### 2.配置本地git仓库

#### 1.创建”README.md“文件
    #该文件是项目描述文件，可以在文件中添加项目的描述、使用方法等，也包括项目的版本信息、作者信息等
#### 2.创建”gitignore“文件
##### 该文件是追踪/忽略设置文件，
    ```
        1.直接写/* 表示忽略目录下的所有文件
            如：build/* 表示生成输出的文件忽略追踪，可以保护git仓库不会非常臃肿
        2.写！+文件名 表示追踪该文件，也是最常见的操作，指定某个路径或文件处于追踪状态，后续可以修改这个文件
            如：!README.md 表示追踪README.md文件，可以保护git仓库不会丢失该文件
    ```
### 3.连接github仓库
#### 1.在github上创建一个仓库，如：xx-xxx
#### 2.创建一个新的分支
    我这里按照我的习惯，默认不用main分支
#### 3.先执行：git init指令
    执行结束后用ls -Force指令查看是否创建成功，git文件夹是否存在：该指令会自动创建一个隐藏文件夹”.git“，用force可以查看是否创建成功
#### 4.在本地git登记远程仓库信息：
    ```
    执行”git remote add origin git@github.com/用户名/仓库名.git“指令
        1.以本项目为例：git remote add origin git@github.com:kkk1233dd/funny-thing.git
        2.这个指令是修改本地git仓库记录的远程仓库文件地址，不会修改任何文件，甚至不会联网 
    ```
#### 5.下载远程仓库分支:git fetch origin
    ```
        1.git fetch origin 正式联网；从远程下载该分支的历史记录，并保存在git中——不会修改当前文件
        2.用git branch -r验证获取成功
    ```
#### 6.正式连接：git checkout -b Notes origin/Notes
    ```
        1.-b表示可以边连边创建新分支，在这里的作用，是在本地git上创建一个分支，基于origin/Notes分支
        2.git checkout 切换分支，直接切换到新的分支上去
        3.用git branch -VV验证连接成功 
        4.-VV表示显示更多内容，这里是显示”分支名 + 最新提交 + 跟踪关系 ： Notes e04cc2b [origin/Notes]“
     ```
#### 7.提交本地修改到远程仓库
    ```
        1.git add . 表示追踪所有文件
        2.git commit -m "提交信息" 表示提交修改，提交信息为”提交信息“
        3.git push origin Notes 表示将本地Notes分支提交到远程仓库Notes分支
    ```